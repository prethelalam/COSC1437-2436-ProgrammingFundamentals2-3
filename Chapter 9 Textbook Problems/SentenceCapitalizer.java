public class SentenceCapitalizer { //come back too
    public static void CapitalizeSentences(String sentence)
    {
        StringBuilder capitalizedSentences = new StringBuilder(sentence);

        // if(capitalizedSentences.isLowerCase()
        // {

        // }

        capitalizedSentences.setCharAt(0, capitalizedSentences.toUpperCase());

        for(int i = 0; i < sentence.length(); i++)
        {
            if(Character.isLowerCase(sentence.toUpperCase(i))
            {

            }
        }

        //see the index of each whitespace, use that index+1 to make the next letter uppercase
    }
}
